/**
 * Length of the longest substring without 
 * repeating characters
*/
#include<bits/stdc++.h>
using namespace std;

int main(){
char a[]="abcdefcghijkzyxz";
int n=strlen(a);
int current_len=1;
int max_len=1;
int visited[256];
for(int i=0;i<256;i++)
{
    visited[i]=-1;
}
return 0;
}